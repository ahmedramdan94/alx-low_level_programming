#include "main.h"

/**
 * print_alphabet - make func to lowercase alphabet from a to z
 * Return: Always (0)
 */

void print_alphabet(void)
{

int alpha;
for (alpha = 'a'; i < 'z'; alpha++)
	_putchar(alpha);
_putchar('\n');
return (0);
}
